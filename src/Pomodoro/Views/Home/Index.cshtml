@model IEnumerable<Pomodoro.Database.Tomato>

@{
    ViewBag.Title = "Blogs";
}

<h2>Tomatoes</h2>

<p>
    <a asp-controller="Home" asp-action="Create">Create New</a>
</p>

<table class="table">
    <tr>
        <th>Id</th>
        <th>Url</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartedAt)
            </td>
        </tr>
    }
</table>